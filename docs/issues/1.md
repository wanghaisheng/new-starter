
Build Error

Error:   × You're importing a component that needs `useState`. This React hook only works in a client component. To fix, mark the file (or its parent) with the `"use client"` directive.

./src/mobile/components/cards/CardStack.tsx

Error:   × You're importing a component that needs `useState`. This React hook only works in a client component. To fix, mark the file (or its parent) with the `"use client"` directive.
  │ 
  │  Learn more: https://nextjs.org/docs/app/api-reference/directives/use-client
  │ 
  │ 
   ╭─[D:\Download\audio-visual\heytcm\new-starter\src\mobile\components\cards\CardStack.tsx:1:1]
 1 │ import { useState } from 'react';
   ·          ────────
 2 │ import { SwipeCard } from '@/mobile/components/cards/SwipeCard';
 3 │ import { User } from '@/core/models/user';
 3 │ import { getRecommendedUsers } from '@/core/models/mock-data';
   ╰────
This error occurred during the build process and can only be dismissed by fixing the error.

1
2

1/1

Next.js 15.2.4



 3 │ import { getRecommendedUsers } from '@/core/models/mock-data';

类似的 我们应该使用数据服务接口来访问数据，这样当我们通过env确定是mock、local、prod阶段即可，正确加载对应的数据，无需修改其他内容



