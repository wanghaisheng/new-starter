# 可访问性设计提示词模板

## 键盘导航设计

```markdown
请设计以下组件的键盘导航方案：

1. 焦点管理策略
2. 键盘快捷键
3. 焦点顺序设计
4. 焦点样式优化
5. 屏幕阅读器支持

组件需求：
[描述组件需求]

设计要求：
- 导航方式：[具体方式]
- 快捷键：[快捷键列表]
- 焦点样式：[样式要求]
- 兼容性：[兼容要求]
```

## 屏幕阅读器支持

```markdown
请设计以下组件的屏幕阅读器支持方案：

1. ARIA属性设计
2. 语义化标记
3. 文本替代方案
4. 状态通知机制
5. 导航优化方案

组件需求：
[描述组件需求]

设计要求：
- 阅读器类型：[阅读器列表]
- 语义要求：[具体要求]
- 状态通知：[通知要求]
- 导航要求：[导航要求]
```

## 颜色对比度优化

```markdown
请优化以下界面的颜色对比度：

1. 文本对比度
2. 图标对比度
3. 状态指示对比度
4. 交互元素对比度
5. 背景对比度

界面需求：
[描述界面需求]

设计要求：
- 对比度标准：[具体标准]
- 颜色主题：[主题要求]
- 状态指示：[指示要求]
- 可读性：[可读性要求]
```

## 表单可访问性设计

```markdown
请设计以下表单的可访问性方案：

1. 标签关联
2. 错误提示
3. 键盘操作
4. 状态反馈
5. 辅助技术支持

表单需求：
[描述表单需求]

设计要求：
- 表单类型：[表单类型]
- 验证要求：[验证要求]
- 错误处理：[处理要求]
- 辅助支持：[支持要求]
```

## 多媒体可访问性设计

```markdown
请设计以下多媒体的可访问性方案：

1. 替代文本
2. 字幕支持
3. 音频描述
4. 控制可访问性
5. 交互优化

多媒体需求：
[描述多媒体需求]

设计要求：
- 媒体类型：[媒体类型]
- 替代方案：[替代要求]
- 控制要求：[控制要求]
- 交互要求：[交互要求]
```

## 动态内容可访问性设计

```markdown
请设计以下动态内容的可访问性方案：

1. 状态变化通知
2. 加载状态提示
3. 错误处理机制
4. 更新通知策略
5. 交互优化方案

动态内容：
[描述动态内容]

设计要求：
- 更新频率：[具体频率]
- 通知方式：[通知要求]
- 错误处理：[处理要求]
- 交互优化：[优化要求]
``` 