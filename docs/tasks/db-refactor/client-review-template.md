# [客户端名称] 代码审查报告

**审查日期**：YYYY-MM-DD  
**审查人员**：[名字]  
**审查版本**：[Git 提交哈希或版本号]

## 审查摘要

[简要描述客户端实现的当前状态、主要优点和存在的问题]

## 一致性检查清单结果

### 文件结构与命名约定

- [ ] 客户端实现位于 `src/core/lib/db/clients/<client-type>/` 目录下
- [ ] 主客户端文件命名为 `<client-type>-client.ts`
- [ ] 包含 `index.ts` 文件，用于导出客户端和相关类型
- [ ] 包含 `README.md` 文件，详细说明客户端的特性和用法
- [ ] 配置接口命名为 `<ClientType>Config`
- [ ] 客户端类命名为 `<ClientType>Client`

**备注**：[文件结构相关问题和建议]

### 类结构与继承

- [ ] 继承自 `BaseClient` 基类
- [ ] 实现 `IDatabaseClient` 接口
- [ ] 配置接口正确继承 `DatabaseConfig`
- [ ] 类成员遵循一致的可见性修饰符
- [ ] 遵循统一的成员变量命名规则

**备注**：[类结构相关问题和建议]

### 构造函数和初始化

- [ ] 构造函数接收类型化的配置对象
- [ ] 构造函数调用 `super()` 初始化基类
- [ ] 构造函数中记录配置信息
- [ ] `initialize()` 方法检查是否已初始化
- [ ] `initialize()` 方法触发 `initialized` 事件
- [ ] `initialize()` 方法使用标准化的错误处理

**备注**：[初始化相关问题和建议]

### 核心方法实现

- [ ] 实现所有 `IDatabaseClient` 接口定义的方法
- [ ] `findById` 方法包含类型参数和正确的返回类型
- [ ] `findAll` 方法支持过滤条件参数
- [ ] `query` 方法支持 `QueryOptions` 参数并返回 `QueryResult`
- [ ] `create`、`update`、`delete` 方法使用一致的参数结构
- [ ] 所有方法在操作前都调用 `checkInitialized()`
- [ ] 表特定方法实现一致

**备注**：[核心方法相关问题和建议]

### 事务支持

- [ ] 实现 `beginTransaction`、`commitTransaction`、`rollbackTransaction` 方法
- [ ] 提供 `transaction` 方法支持回调模式
- [ ] 事务操作具有原子性
- [ ] 事务失败时正确回滚操作
- [ ] 支持嵌套事务或明确说明不支持

**备注**：[事务相关问题和建议]

### 批量操作

- [ ] 实现 `batch` 方法支持批量操作
- [ ] 批量操作支持 add/put/delete 操作类型
- [ ] 批量操作在单个事务中执行
- [ ] 处理批量操作中的错误情况
- [ ] 批量操作后更新相关缓存

**备注**：[批量操作相关问题和建议]

### 错误处理和日志记录

- [ ] 使用 `DatabaseError` 类型表示错误
- [ ] 使用 `DatabaseErrorCode` 枚举表示错误代码
- [ ] 所有方法都包含 try/catch 结构
- [ ] 使用 `createError` 方法创建标准化错误
- [ ] 使用 `logger` 记录操作日志和错误
- [ ] 错误消息提供足够的上下文信息
- [ ] 错误传播回上层应用时保留原始信息

**备注**：[错误处理相关问题和建议]

### 性能优化

- [ ] 实现 `measurePerformance` 方法跟踪操作性能
- [ ] 触发 `performance` 事件提供性能指标
- [ ] 提供可配置的缓存机制
- [ ] 实现索引优化（如适用）
- [ ] 记录性能关键指标的日志

**备注**：[性能优化相关问题和建议]

### 特定客户端功能扩展

- [ ] 特定功能作为标准接口的扩展，不影响核心接口
- [ ] 特定功能有清晰的文档和类型定义
- [ ] 特定配置选项有合理的默认值
- [ ] 扩展功能遵循与核心方法相同的错误处理模式
- [ ] 特定功能的实现考虑性能和资源使用

**备注**：[特定功能相关问题和建议]

### 类型安全

- [ ] 使用泛型支持类型安全的返回值
- [ ] 正确处理日期和复杂对象的序列化/反序列化
- [ ] 避免不必要的类型断言
- [ ] 提供完整的类型导出
- [ ] 方法参数都有明确的类型定义

**备注**：[类型安全相关问题和建议]

### 文档和注释

- [ ] 所有公共方法、类和接口有 JSDoc 注释
- [ ] 注释包含参数、返回值和异常描述
- [ ] README 文档包含安装、配置和示例代码
- [ ] 文档说明客户端的限制和最佳实践
- [ ] 文档提供性能优化建议

**备注**：[文档相关问题和建议]

### 测试覆盖

- [ ] 单元测试覆盖所有核心功能
- [ ] 包含错误处理和边界条件测试
- [ ] 测试事务和回滚功能
- [ ] 测试批量操作功能
- [ ] 测试特定客户端的扩展功能
- [ ] 测试覆盖率达到80%以上

**备注**：[测试相关问题和建议]

## 发现的问题

### 高优先级问题

1. [问题描述]
   - **影响**：[问题影响]
   - **修复建议**：[修复建议]

2. [问题描述]
   - **影响**：[问题影响]
   - **修复建议**：[修复建议]

### 中优先级问题

1. [问题描述]
   - **影响**：[问题影响]
   - **修复建议**：[修复建议]

2. [问题描述]
   - **影响**：[问题影响]
   - **修复建议**：[修复建议]

### 低优先级问题

1. [问题描述]
   - **影响**：[问题影响]
   - **修复建议**：[修复建议]

2. [问题描述]
   - **影响**：[问题影响]
   - **修复建议**：[修复建议]

## 改进建议

1. [建议描述]
   - **理由**：[建议理由]
   - **实施步骤**：[实施步骤]

2. [建议描述]
   - **理由**：[建议理由]
   - **实施步骤**：[实施步骤]

## 总体评估

### 优点

- [优点1]
- [优点2]
- [优点3]

### 不足

- [不足1]
- [不足2]
- [不足3]

### 评分

| 评估维度 | 得分 (1-5) | 备注 |
|----------|------------|------|
| 代码质量 |            |      |
| 一致性   |            |      |
| 文档完整性 |          |      |
| 错误处理 |            |      |
| 性能优化 |            |      |
| 类型安全 |            |      |
| 测试覆盖 |            |      |
| **总体评分** |        |      |

## 结论和下一步行动

[总结审查结果，列出需要采取的下一步行动以及建议的时间表] 