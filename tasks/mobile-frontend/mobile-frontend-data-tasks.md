# 移动端前端和数据开发任务计划

## 概述

本文档定义了移动端前端和数据开发相关的任务，基于对原型(prototype.html)与当前实现的对比分析，以及项目README.md中的技术要求。开发将分为三个环境阶段：

1. **Mock阶段**：使用JSON+fake IndexedDB模拟数据
2. **Local阶段**：使用本地IndexedDB存储
3. **Production阶段**：使用Firebase+IndexedDB实现在线/离线数据同步

## 1. 原型与现有实现对比分析

### 1.1 已实现的功能

- 基本的页面路由结构 (`app/mobile/`目录)
- 底部导航栏组件 (BottomNavBar)
- 卡片滑动组件 (SwipeCard, CardStack)
- 消息列表和聊天界面基础组件
- 用户资料页面基础结构

### 1.2 需要完善的功能

- **认证流程**：完善登录和注册页面，与原型对齐
- **个人资料设置**：完善照片上传和个人信息编辑功能
- **匹配界面**：实现匹配成功的动画和交互
- **聊天功能**：完善实时聊天和消息通知
- **发现页面**：优化卡片滑动体验和用户信息展示
- **UI美化**：实现原型中的玻璃卡片效果和渐变背景

### 1.3 图片资源需求

需要从原型中提取并下载以下图片资源：

- 用户头像图片 (从randomuser.me和unsplash.com)
- 界面图标和SVG资源
- 背景图片和占位图

## 2. 图片资源下载与管理任务

### 任务2.1：下载原型中的图片资源

**描述**：从prototype.html中提取所有图片URL，下载到本地对应目录

**步骤**：
1. 从原型中提取所有图片URL (randomuser.me和unsplash.com)
2. 下载用户头像图片到`public/assets/images/profiles/`
3. 下载背景和示例图片到`public/assets/images/backgrounds/`
4. 将SVG图标转换为独立文件存储在`src/assets/images/icons/`

**完成标准**：
- 所有原型中使用的图片资源可在本地访问
- 图片资源组织合理，便于在应用中引用

### 任务2.2：创建图片资源索引

**描述**：创建图片资源索引文件，便于在应用中统一管理和引用图片

**步骤**：
1. 创建`src/assets/images/index.ts`文件
2. 导出所有图片资源的路径常量
3. 按类别组织（头像、背景、图标等）

**完成标准**：
- 可通过导入`@/assets/images`访问所有图片资源
- 资源路径使用类型安全的常量

## 3. 移动端UI组件开发任务

### 任务3.1：实现玻璃卡片组件

**描述**：实现原型中的玻璃卡片效果(glass-card)，作为可复用组件

**步骤**：
1. 创建`src/mobile/components/ui/GlassCard.tsx`组件
2. 实现backdrop-filter和背景模糊效果
3. 支持自定义圆角、边框和透明度

**完成标准**：
- 组件外观与原型中的玻璃卡片效果一致
- 组件支持自定义属性和子元素

### 任务3.2：优化卡片滑动组件

**描述**：完善现有的SwipeCard组件，实现更流畅的滑动效果和动画

**步骤**：
1. 优化`src/mobile/components/cards/SwipeCard.tsx`
2. 添加滑动方向指示器和动画
3. 实现照片轮播功能（支持多张照片）
4. 添加照片指示器（原型中的小圆点）

**完成标准**：
- 滑动体验流畅，有明确的视觉反馈
- 支持多张照片的轮播和指示器

### 任务3.3：实现匹配成功界面

**描述**：实现原型中的匹配成功界面，包括动画效果

**步骤**：
1. 创建`src/mobile/components/matches/MatchSuccess.tsx`组件
2. 实现头像动画和文字效果
3. 添加操作按钮（发送消息、继续滑动）

**完成标准**：
- 界面外观与原型一致
- 动画效果流畅
- 按钮功能正常工作

### 任务3.4：完善聊天界面组件

**描述**：优化聊天界面组件，实现更好的用户体验

**步骤**：
1. 优化`src/mobile/components/messages/RealTimeChat.tsx`
2. 改进消息气泡样式和布局
3. 添加消息状态指示（已发送、已读）
4. 实现输入框自动增高

**完成标准**：
- 聊天界面外观与原型一致
- 消息状态指示正确显示
- 输入体验流畅

### 任务3.5：实现测试类型选择页面

**描述**：实现测试类型选择入口页面，支持多种测试类型的选择和展示

**架构要求**：
1. 使用 `DataServiceFactory` 获取数据服务实例
2. 实现适当的错误处理和日志记录
3. 支持离线操作和数据同步
4. 遵循项目的类型安全规范

**步骤**：
1. 创建`app/mobile/tests/page.tsx`作为测试类型选择入口
2. 实现测试类型卡片展示组件
3. 为每种测试类型创建独立的测试页面：
   - 八字命理测试 (`app/mobile/tests/bazi/page.tsx`)
   - 中医体质测试 (`app/mobile/tests/tcm/page.tsx`)
   - MBTI性格测试 (`app/mobile/tests/mbti/page.tsx`)
   - 五行性格测试 (`app/mobile/tests/wuxing/page.tsx`)
   - 灵魂伴侣测试 (`app/mobile/tests/soulmate/page.tsx`)
4. 实现测试结果展示页面
5. 添加测试进度保存和恢复功能

**完成标准**：
- 测试类型选择页面布局美观，易于使用
- 每种测试类型都有独立的测试流程
- 测试结果能够正确保存和展示
- 支持测试进度的保存和恢复
- 符合项目的架构和最佳实践要求
- 通过所有单元测试和集成测试

### 任务3.6：更新匹配页面

**描述**：更新匹配页面以支持新的测试类型和匹配算法

**架构要求**：
1. 使用 `DataServiceFactory` 获取数据服务实例
2. 实现多维度匹配算法
3. 优化性能和用户体验
4. 实现数据同步和冲突解决
5. 添加适当的错误处理和日志记录

**步骤**：
1. 更新`app/mobile/matches/page.tsx`
2. 实现多维度匹配算法：
   - 八字命理匹配
   - 中医体质匹配
   - MBTI性格匹配
   - 五行性格匹配
   - 灵魂伴侣匹配
3. 优化匹配结果展示
4. 添加匹配度分析详情
5. 实现数据同步和冲突解决机制

**完成标准**：
- 匹配页面支持所有测试类型的匹配结果展示
- 匹配算法准确反映用户测试结果
- 匹配度分析清晰易懂
- 性能优化达到要求（页面加载时间<2秒）
- 支持离线操作和数据同步
- 符合项目的架构和最佳实践要求
- 通过所有单元测试和集成测试

### 任务3.7：更新设置页面

**描述**：更新设置页面以支持测试相关的配置和偏好设置

**架构要求**：
1. 使用 `DataServiceFactory` 获取数据服务实例
2. 实现数据持久化和同步
3. 支持离线操作
4. 添加适当的错误处理和日志记录
5. 实现用户友好的错误提示

**步骤**：
1. 更新`app/mobile/settings/page.tsx`
2. 添加测试相关设置：
   - 测试结果隐私设置
   - 匹配偏好设置
   - 测试提醒设置
3. 实现测试历史记录管理
4. 添加测试结果导出功能
5. 实现数据同步和冲突解决机制

**完成标准**：
- 设置页面包含所有测试相关的配置选项
- 用户能够管理测试历史和结果
- 支持测试结果的导出和分享
- 支持离线操作和数据同步
- 符合项目的架构和最佳实践要求
- 通过所有单元测试和集成测试

## 4. 数据库配置与实现任务

### 任务4.1：配置Mock环境（JSON+fake IndexedDB）

**描述**：配置开发环境使用JSON文件作为主数据源，fake-indexeddb模拟客户端离线存储

**步骤**：
1. 更新`.env.development`配置：
   ```
   NEXT_PUBLIC_DATABASE_ENV=mock
   NEXT_PUBLIC_MOCK_DB_TYPE=json
   NEXT_PUBLIC_USE_FAKE_INDEXEDDB=true
   NEXT_PUBLIC_MOCK_DB_JSON_PATH=./data/mock-db.json
   NEXT_PUBLIC_MOCK_DB_AUTOSAVE=true
   ```
2. 创建或更新`data/mock-db.json`文件，添加测试数据
3. 确保`MockDatabaseClient`和`MockIndexedDBClient`正确配置

**完成标准**：
- 应用启动时正确加载JSON数据
- 数据变更自动保存到JSON文件
- fake-indexeddb正确模拟客户端离线存储

### 任务4.2：实现用户数据模型和服务

**描述**：完善用户数据模型和服务，支持个人资料管理

**步骤**：
1. 更新`src/core/lib/db/models/user.ts`，确保包含所有必要字段
2. 完善`src/core/services/user-service.ts`，添加个人资料管理方法
3. 实现照片上传和管理功能

**完成标准**：
- 用户模型包含原型中展示的所有字段
- 服务方法支持所有必要的用户操作
- 照片上传和管理功能正常工作

### 任务4.3：实现匹配和聊天数据服务

**描述**：完善匹配和聊天相关的数据模型和服务

**步骤**：
1. 更新`src/core/lib/db/models/match.ts`和`message.ts`
2. 完善`src/core/services/match-service.ts`和`message-service.ts`
3. 实现实时消息更新机制

**完成标准**：
- 匹配和消息模型完整
- 服务方法支持所有必要操作
- 实时消息更新机制正常工作

### 任务4.4：配置Local环境（IndexedDB）

**描述**：配置本地开发环境使用IndexedDB作为主要存储

**步骤**：
1. 创建`.env.local`配置：
   ```
   NEXT_PUBLIC_DATABASE_ENV=local
   NEXT_PUBLIC_LOCAL_DB_TYPE=indexeddb
   NEXT_PUBLIC_DB_NAME=app_database_local
   NEXT_PUBLIC_DB_VERSION=1
   ```
2. 确保`IndexedDBClient`正确配置
3. 实现数据迁移脚本，从mock数据迁移到IndexedDB

**完成标准**：
- 应用可以使用IndexedDB存储和检索数据
- 数据迁移脚本正常工作

### 任务4.5：配置Production环境（Firebase+IndexedDB）

**描述**：配置生产环境使用Firebase作为远程数据源，IndexedDB作为离线存储

**步骤**：
1. 创建或更新`.env.production`配置：
   ```
   NEXT_PUBLIC_DATABASE_ENV=production
   NEXT_PUBLIC_CLOUD_DB_TYPE=firebase
   NEXT_PUBLIC_USE_FIREBASE=true
   NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
   NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain
   NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
   NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket
   NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
   NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
   ```
2. 确保`FirebaseClient`正确配置
3. 实现在线/离线数据同步机制

**完成标准**：
- 应用可以连接到Firebase并同步数据
- 离线时可以使用IndexedDB继续工作
- 恢复在线时自动同步数据

### 任务4.6：修复Repository类型安全

**描述**：修复仓储方法返回类型不一致的问题，确保类型安全

**步骤**：
1. 更新`BaseRepository`类中的查询方法，统一从`QueryResult`中提取`data`
2. 修复所有具体仓储实现，包括`UserRepository`、`MatchRepository`、`MessageRepository`等
3. 更新特殊方法的返回类型处理，如`findById`和`findByUsers`

**完成标准**：
- 所有仓储方法返回类型与接口定义一致
- 客户端代码使用仓储方法时无类型错误
- 确保从`QueryResult`中正确提取数据

### 任务4.7：优化数据库配置系统

**描述**：实现更加灵活和类型安全的数据库配置系统

**步骤**：
1. 创建专用类型文件，分离接口定义
2. 实现数据库配置构建器模式
3. 为不同环境实现专用配置选项

**完成标准**：
- 配置系统更加模块化和可维护
- 配置对象具有类型安全性
- 环境之间的配置转换工作正常

### 任务4.8：实现SQLite适配器优化

**描述**：优化移动端SQLite适配器，提高性能和可靠性

**步骤**：
1. 创建标准化的SQLite配置文件和接口
2. 改进与Capacitor SQLite插件的集成
3. 实现平台检测工具，提供可靠的平台识别

**完成标准**：
- SQLite在移动环境中工作稳定
- 配置选项支持多样化需求
- 平台检测正确识别Web、Android和iOS环境

## 5. 页面实现任务

### 任务5.1：完善认证页面

**描述**：实现与原型一致的登录和注册页面

**步骤**：
1. 更新`app/mobile/auth/login/page.tsx`和`phone/page.tsx`
2. 实现手机号和Google登录功能
3. 添加表单验证

**完成标准**：
- 页面外观与原型一致
- 登录和注册功能正常工作

### 任务5.2：完善个人资料设置页面

**描述**：实现与原型一致的个人资料设置页面

**步骤**：
1. 更新`app/mobile/profile/setup/page.tsx`和`edit/page.tsx`
2. 实现照片上传和管理界面
3. 实现个人信息编辑表单

**完成标准**：
- 页面外观与原型一致
- 照片上传和管理功能正常工作
- 个人信息编辑功能正常工作

### 任务5.3：完善发现页面

**描述**：优化发现页面，实现与原型一致的卡片滑动界面

**步骤**：
1. 更新`app/mobile/discover/page.tsx`
2. 集成优化后的卡片滑动组件
3. 实现滑动操作和匹配逻辑

**完成标准**：
- 页面外观与原型一致
- 卡片滑动和匹配功能正常工作

### 任务5.4：完善聊天页面

**描述**：优化聊天列表和对话页面

**步骤**：
1. 更新`app/mobile/matches/messages/page.tsx`和`[id]/page.tsx`
2. 集成优化后的聊天组件
3. 实现消息搜索和过滤功能

**完成标准**：
- 页面外观与原型一致
- 聊天功能正常工作
- 消息搜索和过滤功能正常工作

## 6. 测试与优化任务

### 任务6.1：编写单元测试

**描述**：为关键组件和服务编写单元测试

**步骤**：
1. 为UI组件编写测试用例
2. 为数据服务编写测试用例
3. 确保测试覆盖率达到要求
4. 为仓储方法编写测试，验证QueryResult处理逻辑

**完成标准**：
- 测试覆盖率达到80%以上
- 所有测试用例通过
- 仓储方法的返回类型正确（确保从QueryResult中提取data）

### 任务6.2：性能优化

**描述**：优化应用性能，特别是数据加载和UI渲染

**步骤**：
1. 实现数据预加载和缓存策略
2. 优化组件渲染性能
3. 减少不必要的重渲染
4. 优化SQLite操作，特别是在移动环境中

**完成标准**：
- 应用启动时间小于2秒
- 页面切换流畅，无明显卡顿
- 数据加载有适当的加载状态指示
- SQLite操作响应时间合理（移动端查询<500ms）

### 任务6.3：离线功能测试

**描述**：测试应用在离线状态下的功能

**步骤**：
1. 测试离线数据访问
2. 测试离线操作后的数据同步
3. 测试网络状态变化时的应用行为
4. 测试跨平台场景中的离线行为一致性

**完成标准**：
- 离线状态下可以访问已缓存的数据
- 离线操作后恢复网络连接时数据正确同步
- 网络状态变化时有适当的用户提示
- 在Web、Android和iOS平台上行为一致

### 任务6.4：类型安全测试

**描述**：测试数据库接口的类型安全性，特别是仓储模式实现和QueryResult处理

**步骤**：
1. 测试仓储方法的返回类型是否符合预期
2. 测试数据库配置系统的类型安全性
3. 测试混合客户端同步配置的正确性
4. 测试平台检测工具在不同环境中的行为

**完成标准**：
- 仓储方法返回类型与接口定义一致
- 不同环境之间的配置转换正确
- 混合客户端能够正确处理同步配置
- 平台检测工具正确识别Web、Android和iOS环境

## 7. 部署与发布任务

### 任务7.1：配置生产环境

**描述**：准备生产环境配置和部署脚本

**步骤**：
1. 完善`.env.production`配置
2. 创建部署脚本
3. 配置CI/CD流程

**完成标准**：
- 生产环境配置完整
- 部署脚本可以正常工作
- CI/CD流程配置正确

### 任务7.2：移动应用打包

**描述**：使用Capacitor打包移动应用

**步骤**：
1. 配置Capacitor
2. 生成Android和iOS应用包
3. 测试打包后的应用

**完成标准**：
- Android和iOS应用包生成成功
- 打包后的应用功能正常

## 8. 文档与知识库任务

### 任务8.1：更新开发文档

**描述**：更新项目文档，反映最新的开发状态和指南

**步骤**：
1. 更新README.md
2. 更新开发指南
3. 更新API文档

**完成标准**：
- 文档内容准确反映当前项目状态
- 文档格式清晰，易于理解

### 任务8.2：创建用户指南

**描述**：创建应用用户指南

**步骤**：
1. 编写用户功能说明
2. 创建常见问题解答
3. 添加截图和使用示例

**完成标准**：
- 用户指南覆盖所有主要功能
- 指南内容清晰，易于理解

## 时间线与优先级

### 第一阶段（Mock数据开发）

| 任务ID | 任务名称 | 优先级 | 预计工时 |
|--------|---------|-------|----------|
| 2.1 | 下载原型中的图片资源 | 高 | 1天 |
| 2.2 | 创建图片资源索引 | 中 | 0.5天 |
| 3.1 | 实现玻璃卡片组件 | 高 | 1天 |
| 3.2 | 优化卡片滑动组件 | 高 | 2天 |
| 4.1 | 配置Mock环境 | 高 | 1天 |
| 4.2 | 实现用户数据模型和服务 | 高 | 2天 |
| 4.6 | 修复Repository类型安全 | 高 | 1天 |
| 4.7 | 优化数据库配置系统 | 中 | 1.5天 |
| 4.8 | 实现SQLite适配器优化 | 中 | 1.5天 |

### 第二阶段（Local数据开发）

| 任务ID | 任务名称 | 优先级 | 预计工时 |
|--------|---------|-------|----------|
| 3.3 | 实现匹配成功界面 | 中 | 1.5天 |
| 3.4 | 完善聊天界面组件 | 中 | 2天 |
| 4.3 | 实现匹配和聊天数据服务 | 高 | 2天 |
| 4.4 | 配置Local环境 | 高 | 1天 |
| 5.1 | 完善认证页面 | 中 | 1.5天 |
| 5.2 | 完善个人资料设置页面 | 中 | 2天 |

### 第三阶段（Production数据开发）

| 任务ID | 任务名称 | 优先级 | 预计工时 |
|--------|---------|-------|----------|
| 4.5 | 配置Production环境 | 高 | 2天 |
| 5.3 | 完善发现页面 | 中 | 1.5天 |
| 5.4 | 完善聊天页面 | 中 | 1.5天 |
| 6.1 | 编写单元测试 | 中 | 3天 |
| 6.2 | 性能优化 | 中 | 2天 |
| 6.3 | 离线功能测试 | 高 | 1.5天 |
| 6.4 | 类型安全测试 | 高 | 1天 |

### 第四阶段（部署与发布）

| 任务ID | 任务名称 | 优先级 | 预计工时 |
|--------|---------|-------|----------|
| 7.1 | 配置生产环境 | 高 | 1天 |
| 7.2 | 移动应用打包 | 高 | 1.5天 |
| 8.1 | 更新开发文档 | 中 | 1天 |
| 8.2 | 创建用户指南 | 低 | 1天 |

## 总结

本任务计划涵盖了移动端前端和数据开发的各个方面，从UI组件开发到数据库配置，从页面实现到测试优化。通过分阶段实施，可以确保开发过程有序进行，并在每个阶段都有可交付的成果。

重点任务包括：
1. **UI组件开发**：实现与原型一致的用户界面和交互体验
2. **数据模型与服务**：建立健壮的数据访问层
3. **数据库配置与优化**：支持多环境、跨平台的数据存储方案
4. **类型安全增强**：确保仓储方法返回类型一致，提高代码质量
5. **平台检测**：实现可靠的平台检测机制，支持Web、Android和iOS环境
6. **SQLite适配**：优化移动端数据库性能和可靠性

开发过程将遵循README.md中描述的数据存储策略：
- **Mock阶段**：使用JSON文件作为主数据源，fake-indexeddb模拟客户端离线存储
- **Local阶段**：使用IndexedDB作为主要存储
- **Production阶段**：使用Firebase作为远程数据源，IndexedDB作为离线存储

通过这种分层存储架构，结合优化后的仓储模式实现和类型安全增强，可以确保应用在各种网络条件下都能正常工作，并提供良好的用户体验。同时，完善的测试策略将确保代码质量和系统稳定性，特别是在跨平台场景中的一致性表现。